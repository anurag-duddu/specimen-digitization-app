/// The actions family (10 section 4.1): buttons, icon buttons, capsule
/// toggles, chips, segmented controls, badges and key caps.
///
/// Wave 1 slot C1 fills this barrel. It is empty rather than absent so that
/// the top barrel `specimen_ui.dart` is written once and never edited by a
/// family agent.
library;
