/// The overlays family (10 section 4.3): popover menus, tooltips, toasts,
/// banners, disclosures, tabs, sheets and dialogs.
///
/// Wave 1 slot C3 fills this barrel. It is empty rather than absent so that
/// the top barrel `specimen_ui.dart` is written once and never edited by a
/// family agent.
library;
