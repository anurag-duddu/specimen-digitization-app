/// The navigation family (10 section 4.4): the pill navigation, the rail,
/// the sidebar, the top bar and the page scaffold.
///
/// Wave 1 slot C4 fills this barrel. It is empty rather than absent so that
/// the top barrel `specimen_ui.dart` is written once and never edited by a
/// family agent.
library;
