/// The inputs family (10 section 4.2): text fields, text areas, search
/// fields, selects, switches, checkboxes and radios.
///
/// Wave 1 slot C2 fills this barrel. It is empty rather than absent so that
/// the top barrel `specimen_ui.dart` is written once and never edited by a
/// family agent.
library;
