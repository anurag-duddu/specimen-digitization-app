/// The data family (10 section 4.5): list rows, progress, skeletons, empty
/// states, data tiles, arc indicators, avatars and hairlines.
///
/// Wave 1 slot C5 fills this barrel. It is empty rather than absent so that
/// the top barrel `specimen_ui.dart` is written once and never edited by a
/// family agent.
library;
