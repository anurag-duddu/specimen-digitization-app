name: specimen_digitization
description: "Flutter client for Specimen Digitization."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Identity and attestation only. The client never writes to Firebase
  # Storage and never opens a SQL Connect channel: photographs go up through
  # the scoped API's `PUT /uploads/{id}/content` and every record is read and
  # written through the same API (docs/execution/FLUTTER.md, "Upload
  # transport"). `firebase_storage` and `firebase_data_connect` were declared
  # and never imported, so they were shipped weight and attack surface for a
  # capability the product does not use. The SQL Connect schema and its named
  # operations are the server's, and `dataconnect/connector/connector.yaml`
  # declares no `dartSdk` generate target, so nothing here is generated
  # against them either.
  firebase_core: ^4.14.0
  firebase_auth: ^6.6.1
  http: ^1.6.0
  crypto: ^3.0.7
  file_selector: ^1.1.0
  image_picker: ^1.2.3
  shared_preferences: ^2.5.5
  firebase_app_check: ^0.4.7
  camera: ^0.11.0

  # The design system. Owned by this repository, so its version moves with the
  # application rather than with pub.dev.
  specimen_ui:
    path: packages/specimen_ui
  # Pinned: 17.5.0 is the last go_router that resolves on Flutter 3.38.5
  # and Dart 3.10.4. Newer majors raise the Flutter floor.
  go_router: 17.5.0

# firebase_core_web 3.11.0 requires a JS interop API not available in the
# repository's Flutter 3.38 / Dart 3.10 toolchain. Remove this override after
# upgrading to Flutter 3.44 or after FlutterFire publishes a compatible fix.
dependency_overrides:
  firebase_core_web: 3.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # The launcher icons and the splash are generated from assets/brand/ by
  # these two, per 09 section 9. Both are pinned exactly rather than with a
  # caret: they write platform files into android/, ios/ and web/, so a minor
  # release that changes what it writes is a change to the shipped product
  # and belongs in a commit that says so.
  flutter_launcher_icons: 0.14.4
  # 09 names 2.4.8. It depends on meta ^1.18.0 and every flutter_test from
  # this SDK depends on meta 1.17.0, so 2.4.8 does not resolve on Flutter
  # 3.38.5 and 2.4.7 is the last one that does. Same reason as go_router
  # above. Revisit with the SDK, not on its own.
  flutter_native_splash: 2.4.7

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # Every glyph the product draws is a Phosphor face shipped by
  # `specimen_ui`, so nothing here names a Material or a Cupertino icon. The
  # flag stays because the carrier is still a `MaterialApp`: the editing
  # infrastructure `FieldCore` builds on is Material's, and a selection
  # control that reaches for a glyph with no font behind it draws a blank box
  # that no gate would see (10 section 1.3).
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
