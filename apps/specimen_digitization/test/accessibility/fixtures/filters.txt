role=form
  label="Filter the queue"
  label="All filters must match."
    button enabled=true tap label="Why"
  label="Status"
  textField enabled=true tap label="Processing step"
  textField enabled=true tap label="Blocker"
  textField enabled=true tap label="Issue"
  label="Provenance"
  textField enabled=true tap label="Uploaded by"
  textField enabled=true tap label="Upload batch"
  textField enabled=true tap label="Profile"
  textField enabled=true tap label="Profile version"
  label="Dates"
  button enabled=true tap label="Created: Any date"
  label="Risk"
  selected=false enabled=true tap label="Include not measured Turn this off to narrow the queue to a risk range."
  label="Risk range, 0 to 100"
    enabled=false value="0%"
    enabled=false value="100%"
  label="Every record, measured or not."
  label="Identifiers"
  textField enabled=true tap label="Image file"
  textField enabled=true tap label="Processing run"
  button enabled=true tap label="Clear all"
  button enabled=true tap label="Apply"
