role=form
  label="All filters must match."
    button enabled=true tap label="Why"
  label="Status"
  textField enabled=true tap label="Processing step"
  textField enabled=true tap label="Blocker"
  textField enabled=true tap label="Issue"
  label="Provenance"
  textField enabled=true tap label="Uploaded by"
  textField enabled=true tap label="Upload batch"
  textField enabled=true tap label="Profile"
  textField enabled=true tap label="Profile version"
  label="Dates"
  textField enabled=true tap label="Created on or after" hint="The first day the filter keeps."
  textField enabled=true tap label="Created before" hint="The first day the filter leaves out."
  label="Risk"
  selected=true enabled=true tap label="Include not measured"
  label="Turn this off to narrow the queue to a risk range."
  textField enabled=false label="Lowest risk" value="0" hint="Turn off Include not measured to use a risk range."
  textField enabled=false label="Highest risk" value="100" hint="Turn off Include not measured to use a risk range."
  label="Identifiers"
  textField enabled=true tap label="Image file"
  textField enabled=true tap label="Processing run"
