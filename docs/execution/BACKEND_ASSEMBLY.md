# Collection, authority, reading and search assembly checkpoint

Branch `codex/backend-reliability`, worktree `/Users/anuragduddu/.codex/worktrees/3782/specimen-digitization-app`. This is an additive application checkpoint after reliability `a63ab1a` and exact historical digest repair `4cd3595`, not a complete product or production release.

## Provenance

Collection first5023f235, evidence first0da144d and data paging584aac7 were already carried before a63ab1a. New extension commits17a585a4943bc04af334af33a379f9c5cb177355, eef106f71e4144dd314658f89732379c08e440bb,12bd1032b26e6fa2b94213106f3e1b81f2202dcb and9df195575f0fab5ffcefcf85c14326d184464ab4 are carried as471ef78,31d3a84,f3b9a12,806ab8a. Their owner modules remain unchanged except the owner-authored preflight remedy cb968490 carried as ede4f6d. Scanner-only bbf536a is carried as bf2fdc0. Integration should carry its equivalent dependencies once and apply the subsequent core assembly commit.

## Resulting behavior

The workflow classifies/selects from published profile mappings, retains profile snapshots and registry/model/prompt dependencies, records deterministic image measurements without inventing quality approval, and executes all seven typed evidence phases against verified literal evidence. Missing source literals remain review issues. Unknown pricing, missing approved authority configuration, or integrity failures are operational blocks. Authority intent and result receipts surround actual outgoing requests, and pure review phases never repeat requests. Parties name matching requires a fully qualified retained identity selected by a reviewer and a separate approval. Source corrections invalidate affected current authority/phase results and issue new attempt IDs; original artifacts and receipts remain accessible through immutable history.

All API evidence readers recheck current scope/sensitive access and optionally pin an exact revision. `/phases/{phase}`, `/authority-results/{tool_id}?field_key=...`, `/authority-results/{tool_id}/raw?field_key=...`, `/observations/{id}/raw`, `/observations/{id}/metadata`, and `/disagreements/{region_id}` verify stored hashes. Omitting an ambiguous authority field is rejected; selection `after.field_key`, when present, must equal target_id. Raw HTTP responses have a 1 MiB maximum and never silently truncate. This gate runs after blob retrieval and does not bound blob download memory.

Reading artifacts preserve raw codepoint, UTF8 and UTF16 half-open offsets. Metadata declarations are absent unless actually supplied; current adapters do not report language/script, so these remain unknown. Long comparisons exceeding the bounded algorithm's work limits report policy_blocked with null distance and explicit unmeasured risk. Triage is uncalibrated and never clearance authority. The previous silent 8,000-character omission is removed.

Original bytes remain immutable. The authenticated view derivative is pixel-only PNG with EXIF already applied and a retained original-pixel-edge to view-pixel-edge affine transform. ROI coordinates remain original pixel edges; `rotation_quarter_turns` applies clockwise after original-coordinate crop. Backend pixel lineage uses the same crop implementation for execution and integrity checks.

Authorized `POST /images/preflight?collection_id=...` accepts bounded raw bytes and declared Content-Type, returns the typed server_preflight contract and creates no upload/specimen/blob or external-provider effect. UI must require an explicit server-check action before transmitting bytes. Intake independently validates and hashes originals. Optional HEIC/DNG dependencies are not enabled or installed by this checkpoint: codecs default disabled, memory enforcement required, macOS default blocks where enforcement is unavailable. See the codec owner's licensing/runtime report. Intake currently supports validated JPEG/PNG/TIFF only; optional codec preflight does not imply optional format intake/worker support.

`GET /specimens` uses metadata-only SearchSpecimens or the equivalent SQLite JSON projection, AND filters, fixed cutoff and (created_at,id) keyset. Filters: collection_id required; specimen_id,asset_id,active_run_id,batch_id,uploader_id,state,stage,disposition,profile_id,profile_version,reason_code,blocker,created_from inclusive UTC,created_before exclusive UTC,risk_min/max inclusive0..100. Unknown/duplicate filters, invalid enums/ranges and mismatched cursors fail422. Limit1..100,default50. Omitted/0 cursor starts; subsequent cursor is opaque and scope/actor/current-sensitive-permission/filter bound. A full terminal page may require one empty next page. Cursors confer no authorization; every query checks current membership. Changes to mutable filter values during a sweep may alter membership; cutoff freezes creation horizon, not a database snapshot. Supplemental indexes do not promise indexed latency for every JSON predicate.

Current list/detail/workspace created_at uses typed persistence time; domain_created_at exposes the original snapshot timestamp separately. Historical snapshot/run hashes remain unchanged. Source access, run actions and events use exact metadata lookup before loading the matching specimen. Upload checksum duplicate precheck still uses the legacy full-list helper. SQLite has a scoped checksum unique constraint; SQL currently does not. The SQL concurrent-duplicate race is a correctness gap requiring coordinated typed checksum/schema/write changes, not only query optimization.

## Verification record

Actual SQLite metadata pagination exercised10,037 records without Python snapshot hydration, including sensitivity and null-versus-zero risk. Actual local TCP authority fixtures prove intent before outgoing request, name-only ambiguity, rejected foreign identity, qualified selection+separate clearance and source-correction reread. Real PostgreSQL18/SQL Connect on owned loopback5649/9599 passes typed authority review, named search AND filters, matching list/detail revision/persistence timestamps, keyset terminal page, worker recovery and110 repeated HTTP reviews retaining history/replay. Multilingual HTTP artifact tests verify astral UTF16 offsets and9k incompatible readings report policy_blocked rather than agreement. Server preflight checks authorization, body bound, digest and zero persisted records/blobs. All4 clockwise ROI pixel rotations are tested.

Canonical scripts/ci/verify.sh PASS:206 Python passed/13 explicit optional-codec and SQL skips; all repository and secret checks, Flutter analysis,16 widget tests and release web build pass. Log /tmp/specimen-assembly-canonical-final.log. Separately enabled actual API TCP + SQL Connect + TCP Parties test:5 passed; worker/upload/history SQL/TCP regression:19 passed. The exact assembly commit is supplied in the handoff. Regeneration utility `docs/execution/generate_backend_next_fixture.py` requires a fresh output path and never overwrites the reviewed fixture; UUIDs/timestamps intentionally change. The original frozen `backend-wire-examples.json` is unchanged. New `backend-next-wire-examples.json` contains actual synthetic application responses with a local TCP mocked authority, not live institutional access; separate pure Unicode/bounded examples are labeled.

## Open work and limits

Shared provider circuit breaking, complete crash-boundary matrix and sync SAM3/auth total deadline proof remain open. Current bounded async model calls and post-deadline rejection are not SAM3 service validation. A single oversized active evidence graph can still reach the256KiB snapshot bound; audit history compaction does not solve that case. Optional codecs are preflight capabilities only until policy, distribution/runtime approval and full intake/worker pixel mapping are implemented. Language/script declaration capture and adapter propagation remain implementation work; unknown metadata is honest but not complete multilingual support. No live institutionally approved classifier, museum quality calibration, Parties credentials, production schemas, paid inference, cloud provisioning, push, merge or deployment occurred. Independent integrated Flutter/HTTP/SQL acceptance remains required.
